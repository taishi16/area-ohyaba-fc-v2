# ホームページ更新マニュアル

AREA OHYABA FC 公式サイトの内容を変える方法です。
**HTMLやプログラムを触る必要はありません。Googleスプレッドシートを直すだけです。**
スマートフォンからでも更新できます。

---

## いちばん大事なこと

| | |
|---|---|
| **更新する場所** | Googleスプレッドシート「AREA OHYABA FC サイトデータ」 |
| **反映されるまで** | 最大2〜3分（急ぐときは後述の「今すぐ反映する」） |
| **やってはいけないこと** | 1行目（見出しの行）を書き換える・列の順番を変える・シート名を変える |
| **やっていいこと** | 行を足す・行を消す・中身を書き換える・行を並べ替える |

行の順番は気にしなくて大丈夫です。**サイトが日付や状態を見て自動で並べ替えます。**

---

## 1. 試合予定を追加する

シート **MATCHES** を開きます。

1. いちばん下の空いている行に、次のように入れます。

| 列 | 入れるもの | 例 |
|---|---|---|
| match_id | 好きな番号（重複しなければ何でも可） | `M013` |
| date | **必ず 年/月/日** | `2026/09/20` |
| kickoff_time | キックオフ時刻（空でも可） | `10:00` |
| competition | 大会名・節 | `埼玉県3部南部地区リーグ 第12節` |
| opponent | 対戦相手 | `XXXXX FC` |
| home_away | `HOME` か `AWAY`（空でも可） | `HOME` |
| venue | 会場（空でも可） | `大谷場グラウンド` |
| area_score | **まだ空のまま** | |
| opponent_score | **まだ空のまま** | |
| status | **`scheduled`** | `scheduled` |
| note | 備考（空でも可） | |
| sample | **空のまま**（`TRUE`にすると「SAMPLE」表示になります） | |

2. これだけで、次の場所に自動で出ます。
   - トップページの **NEXT MATCH**（いちばん近い1試合）
   - 試合ページの **SCHEDULE**
   - トップページの **MATCHES**

> 日付が過ぎたら、NEXT MATCH からは自動的に消えます。**行を削除する必要はありません。**

---

## 2. 試合結果を入れる

試合が終わったら、シート **MATCHES** のその試合の行を直します。

1. `area_score` に AREA OHYABA の得点（例 `3`）
2. `opponent_score` に相手の得点（例 `1`）
3. `status` を **`scheduled` → `finished`** に変える

これだけで自動的に、

- NEXT MATCH から消える
- **LAST MATCH** になる
- 試合ページの **RESULTS** に入る
- 勝ち負けの表示（WIN / DRAW / LOSE）が自動でつく

### 試合が中止・延期になったとき

`status` を **`cancelled`** にします。得点は空のままで大丈夫です。

### statusは3つだけ

| status | 意味 |
|---|---|
| `scheduled` | これからやる試合 |
| `finished` | 終わった試合（得点も入れる） |
| `cancelled` | 中止・延期 |

---

## 3. 選手を追加する

シート **PLAYERS** の下に1行足します。

| 列 | 入れるもの | 例 |
|---|---|---|
| player_id | 好きな番号 | `P031` |
| number | 背番号 | `9` |
| name | 名前 | `山田 太郎` |
| name_kana | カタカナ | `ヤマダ タロウ` |
| position | `GK` `DF` `MF` `FW` のどれか | `FW` |
| image | **写真のファイル名**（後述） | `players_9` |
| birthday / height / weight / career | 分かる範囲で（空でも可） | |
| profile | ひとこと紹介（空でも可） | |
| active | **`TRUE`** | `TRUE` |
| display_order | 並び順の数字（小さいほど先） | `210` |

選手ページでは **GK → DF → MF → FW** の順に自動でまとまります。

---

## 4. 選手を非表示にする（退団など）

**行を消さないでください。** `active` を **`TRUE` → `FALSE`** に変えるだけです。

- サイトからすぐ消えます
- データは残るので、戻したくなったら `TRUE` に戻すだけです

スタッフ（**STAFF**）・スポンサー（**SPONSORS**）も同じやり方です。

---

## 5. スポンサーを追加する

シート **SPONSORS** に1行足します。

| 列 | 入れるもの | 例 |
|---|---|---|
| sponsor_id | 好きな番号 | `S033` |
| name | 会社名・店名 | `株式会社○○` |
| logo | **ロゴのファイル名**（後述） | `partners_maru` |
| url | ホームページのURL（空なら画像だけ表示） | `https://example.com/` |
| tier | `ユニフォームスポンサー` か `サポートスポンサー` | `サポートスポンサー` |
| active | `TRUE` | `TRUE` |
| display_order | 並び順 | `330` |

`tier` に書いた名前が、そのままサイトの見出しになります。新しい区分を作りたいときは、好きな名前を入れてください。

---

## 6. NEWSを追加する

シート **NEWS** に1行足します。

| 列 | 入れるもの |
|---|---|
| news_id | 好きな番号（例 `N007`） |
| date | `2026/09/20` の形 |
| category | `大会` `スポンサー` `地域活動` `お知らせ` など自由 |
| title | 見出し |
| summary | 一覧に出る短い説明（1〜2行） |
| body | 本文。**改行して何行書いてもOK**（セル内改行は `Alt + Enter`） |
| image | 写真のファイル名（空でも可） |
| url | **外部サイトへ飛ばしたいときだけ**URLを入れる。入れると本文ページを作らず直接そこへ飛びます |
| published | **`TRUE`**（`FALSE` にすると下書き扱いで表示されません） |

新しい順に自動で並びます。**並べ替えは不要です。**

> Instagramの日常投稿を全部NEWSに入れる必要はありません。
> NEWSは「大会出場・新加入・退団・スポンサー・重要なお知らせ・イベント・メディア掲載」など、
> 公式のお知らせだけで十分です。

---

## 7. Instagramの表示を変える（トップページの LATEST ACTIVITY）

トップページに出るInstagram投稿は **3件** です。

1. Instagramアプリで、載せたい投稿を開く
2. 右上（または投稿右下）の共有ボタン →「**リンクをコピー**」
3. シート **ACTIVITY** の `post_url` に貼り付ける

`https://www.instagram.com/p/XXXXXXXX/` という形のURLになっていればOKです。

- 3件分の行が用意してあります。**古い行を書き換えるだけ**で入れ替わります
- 一時的に消したいときは `active` を `FALSE` に
- **Instagramのパスワードや設定は一切不要です**（公式の埋め込み機能を使っています）

---

## 8. 写真を変更する・追加する

写真だけはスプレッドシートに貼れないので、**画像ファイルを所定の場所に置いて、シートにはファイル名を書きます。**

### 置き場所

| 種類 | 置く場所 | シートに書く名前 |
|---|---|---|
| 選手 | `assets/img/players/` | 拡張子とサイズを除いた名前（例: `players_9-480.webp` → `players_9`） |
| スタッフ | `assets/img/staff/` | 同上 |
| スポンサー | `assets/img/sponsors/` | 拡張子を除いた名前（例: `partners_maru.webp` → `partners_maru`） |
| ニュース | `assets/img/news/` | サイズを除いた名前（例: `news_2026_01-800.webp` → `news_2026_01`） |

### いちばんかんたんな手順

1. 写真（スマホで撮ったJPEGでOK）をパソコンの1つのフォルダに集める
2. ファイル名を `players_9.jpg` のように付ける
3. そのフォルダを渡していただければ、**サイト用に軽くしたWebP画像に変換して所定の場所に置きます**
4. シートの `image` 欄に `players_9` と書く

> 写真は**そのままだと1枚3〜7MBあり、スマホでの表示がとても遅くなります**。
> 変換すると1枚30〜80KB程度まで小さくなり、見た目はほぼ変わりません。
> この変換作業だけは年1回程度お願いします（`tools/optimize_images.sh` で自動処理します）。

選手の写真が用意できていないときは、`image` に `players_nowprinting` と書いてください。

---

## 9. クラブの基本情報を変える

シート **CONFIG** の `value` 列を直します。

| key | 意味 |
|---|---|
| `league` | 所属リーグ名（昇格・降格したらここを直す） |
| `home_ground` | 活動拠点 |
| `tagline` | トップに出るスローガン |
| `instagram_url` | Instagramのアドレス |
| `x_url` | Xのアドレス。**空にするとサイトからXのリンクが消えます** |
| `contact_email` | 問い合わせメール。**空にするとメールリンクを出しません** |
| `preview_mode` | `TRUE` の間は画面上部に「PREVIEW」の帯が出ます。**本番公開時に `FALSE`** |
| `show_birthday` | 選手の生年月日を公開するか |
| `show_height` | 選手の身長を公開するか |
| `show_weight` | 選手の体重を公開するか |
| `show_career` | 選手の経歴を公開するか |

`show_〜` を `FALSE` にすると、**その項目はサイトに一切送られません**（隠すのではなく、そもそも出しません）。
シートには情報を残したまま、公開だけ止められます。

---

## 10. 変更がサイトに出ないとき

1. **2〜3分待って、ページを再読み込み**してください（内容を一時的に覚えておく仕組みのためです）
2. それでも変わらないときは、スプレッドシートの上のメニュー
   **「AREA OHYABA」→「今すぐ反映する」** を押してから、ページを再読み込みしてください
3. 入力ミスが心配なときは **「AREA OHYABA」→「入力内容をチェックする」** を押すと、
   日付の書き方や status の間違いを教えてくれます

---

## 11. よくある間違い

| 症状 | 原因 | 直し方 |
|---|---|---|
| 試合が出てこない | 日付が `2026/9/20` でなく `9月20日` などになっている | `2026/09/20` の形にする |
| NEXT MATCH に出ない | `status` が `finished` のまま／日付が過去 | `scheduled` にする・日付を確認 |
| 結果が出ない | `status` が `scheduled` のまま | `finished` にする |
| 選手が出てこない | `active` が空 | `TRUE` と入れる |
| 写真が出ない | ファイル名が違う／ファイルを置いていない | 拡張子・サイズを除いた名前か確認 |
| Instagramが出ない | URLが投稿ページではない（プロフィールURLなど） | `https://www.instagram.com/p/…` の形か確認 |
| 全部消えた | シート名を変えた／1行目の見出しを変えた | 元の名前に戻す |

---

## 12. 困ったときのために

- スプレッドシートは Google が自動で履歴を残しています。
  **「ファイル」→「変更履歴」→「変更履歴を表示」** から、いつでも前の状態に戻せます。
- サイト側でスプレッドシートが読めなかった場合は、**最後に取り込んだ内容を表示し続けます**。
  スプレッドシートを直している途中でも、サイトが真っ白になることはありません。
