/* ==========================================================================
   接続設定 — ここだけ書き換えれば Googleスプレッドシート連携に切り替わる
   --------------------------------------------------------------------------
   AO_API_URL に Google Apps Script のウェブアプリURLを入れると、
   サイトはスプレッドシートの内容を読むようになる。
   空のままなら site/data/site-data.json（同梱データ）を読む。
   ※ 通信に失敗したときは自動的に同梱データへ切り替わるため、
     スプレッドシートが一時的に見られなくてもサイトは表示され続ける。
   ========================================================================== */
window.AO_API_URL = "";
