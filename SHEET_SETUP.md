# スプレッドシート接続の手順（利用者にお願いする作業）

所要時間の目安：**15〜20分**。1回だけの作業です。費用は0円です。

> **この作業をしても、現行サイト `area-ohyaba.sub.jp` には何も起きません。**
> 影響するのは確認用のV2サイトだけです。

準備するもの：**Googleアカウント**（日常的に更新される方のもの）と、パソコン。

---

## STEP 1. スプレッドシートを作る（3分）

1. `sheets/AREA_OHYABA_FC_サイトデータ.xlsx` を **Googleドライブへドラッグ＆ドロップ**でアップロードする
2. アップロードされたファイルを**右クリック** →「**アプリで開く**」→「**Google スプレッドシート**」
3. 開いたら「**ファイル**」→「**Google スプレッドシートとして保存**」
   （画面上部に「Excel形式です」と出ている場合のみ。出ていなければ不要）
4. ファイル名を「**AREA OHYABA FC サイトデータ**」にする

**確認：** 下のタブが8つ（はじめに／試合／ニュース／選手／スタッフ／スポンサー／Instagram／設定）
あればOKです。

> CSVを1つずつ取り込む必要はありません。7シートぶんが1ファイルに入っています。

---

## STEP 2. スクリプトを貼り付ける（5分）

1. スプレッドシートの上のメニューから「**拡張機能**」→「**Apps Script**」
2. 新しいタブが開き、`function myFunction() {}` のような文字が出る
   → **すべて選択して削除**する（Ctrl+A → Delete）
3. `gas/Code.gs` の中身を**全部コピーして貼り付ける**
4. 上の**フロッピーディスクのアイコン（保存）**を押す
5. プロジェクト名を聞かれたら「AREA OHYABA サイト連携」などでOK

---

## STEP 3. 公開設定をする（5分）

1. 同じ画面の右上「**デプロイ**」→「**新しいデプロイ**」
2. 左上の**歯車アイコン**→「**ウェブアプリ**」を選ぶ
3. 次のように設定する

| 項目 | 選ぶもの |
|---|---|
| 説明 | （空欄でOK） |
| **次のユーザーとして実行** | **自分**（自分のメールアドレス） |
| **アクセスできるユーザー** | **全員** |

4. 「**デプロイ**」を押す
5. **「承認が必要です」と出たら**
   - 「アクセスを承認」→ 自分のGoogleアカウントを選ぶ
   - 「このアプリは Google で確認されていません」と出たら
     → 左下の「**詳細**」→「**（プロジェクト名）に移動（安全ではないページ）**」
     → 「**許可**」
   - ※ ご自身が作ったスクリプトなので問題ありません
6. 表示された「**ウェブアプリ**」のURLを**コピー**する
   （`https://script.google.com/macros/s/AKfy........./exec` という長いURLです）

---

## STEP 4. URLを知らせる（1分）

コピーしたURLを、そのまま伝えてください。
こちらでサイトの設定ファイル1か所（`site/assets/js/config.js`）に貼り、再公開します。

```javascript
window.AO_API_URL = "ここに貼ります";
```

**この作業だけはコードを触るため、こちらで行います。以降は一切不要です。**

---

## STEP 5. 入力しやすく整える（1分）

スプレッドシートの画面に戻り、上のメニューに「**AREA OHYABA**」が増えているのを確認します
（出ていなければページを再読み込み）。

「**AREA OHYABA**」→「**はじめに1回だけ：入力しやすく整える**」をクリック。

これで、

- 見出しの行が固定される（スクロールしても見出しが見える）
- 「状態」「表示」「ポジション」などがプルダウンで選べるようになる
- 開催日が `2026/09/20` の形で表示される

---

## STEP 6. 動作を確かめる（3分）

1. 「**試合**」シートを開く
2. いちばん下の空いている行に、テスト用の試合を入れる

```
開催日      2026/12/31
対戦相手    テストFC
```

3. 2〜3分待ってから、サイトを開いて再読み込みする
4. トップページの **NEXT MATCH** が「12.31 vs テストFC」に変わっていれば成功です
5. 確認できたら、その行を削除してください

> すぐ反映したいときは「**AREA OHYABA**」→「**① 今すぐサイトへ反映する**」を押してから再読み込み。

---

## うまくいかないとき

| 症状 | 見るところ |
|---|---|
| サイトの内容が変わらない | 「アクセスできるユーザー」が「**全員**」になっているか |
| メニュー「AREA OHYABA」が出ない | ページを再読み込み。それでも出なければ STEP 2 の保存を確認 |
| デプロイのURLが分からなくなった | 「デプロイ」→「デプロイを管理」から確認できます |
| スクリプトを直したのに反映しない | 「デプロイ」→「デプロイを管理」→ 鉛筆アイコン → バージョンを「新バージョン」にして更新 |

**この設定に失敗しても、サイトは同梱データで問題なく表示され続けます。**
現行サイトにも一切影響しません。安心して試してください。

---

## 誰と共有するか（あとで決めればOK）

スプレッドシートの右上「共有」から、更新をお願いしたい方を追加できます。

| 権限 | 使いどころ |
|---|---|
| **編集者** | 試合結果を入力する方（監督・広報担当など） |
| 閲覧者 | 内容を見るだけの方 |

**Apps Scriptの設定をやり直す必要はありません。** 編集者を足すだけで、その方も更新できます。
