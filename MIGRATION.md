# 本番切替の手順（MIGRATION）

> **現行サイト https://area-ohyaba.sub.jp/ は、この文書の作業を実行するまで一切変更されません。**
> 実行はクラブオーナーの承認後に行います。

---

## 0. 現状の整理

| | 現行サイト | V2プレビュー |
|---|---|---|
| URL | https://area-ohyaba.sub.jp/ | https://taishi16.github.io/area-ohyaba-fc-v2/ |
| 置き場所 | ロリポップ！レンタルサーバー（GMOペパボ） | GitHub Pages（本番はCloudflare Pages予定） |
| 費用 | **月額121円〜が発生している見込み**（無料プランなし） | 0円 |
| 更新方法 | HTMLを直接編集 | Googleスプレッドシート（日本語の表） |
| 検索エンジン | 公開 | **非公開**（`noindex` + `robots.txt` で遮断中） |

`area-ohyaba.sub.jp` は**ロリポップの初期ドメイン**です。ロリポップがDNSを管理しており、
**他社サーバーへ向け直すことはできません。** そのため次の関係になります。

- **現行URLを維持する** ＝ ロリポップの契約を維持する（＝費用が続く）
- **月額0円にする** ＝ URLが変わる

---

## 1. 本番公開の方針（2026-09-06 決定）

**追加費用0円で公開する。現行URL維持のためだけに有料契約を前提にしない。**
本番のホスティングは **Cloudflare Pages を第一候補**とする。

現行 `area-ohyaba.sub.jp` は、**オーナー承認まで一切変更しない。**

### 進め方

1. オーナーがV2の方向性を承認する
2. 公開前チェック（次章）を済ませる
3. Cloudflare Pages へ公開する（`area-ohyaba.pages.dev` など）
4. 次の3点を、そのタイミングで判断する
   - 無料URLへ完全移行するか
   - 現行のロリポップを一定期間残すか（旧URLからの転送のため）
   - 最終的にロリポップを解約するか

### Cloudflare Pages への公開手順（無料）

1. https://dash.cloudflare.com/ で無料アカウントを作る（クレジットカード不要）
2. 「Workers & Pages」→「Create」→「Pages」→「Connect to Git」
3. GitHubの `taishi16/area-ohyaba-fc-v2` を選ぶ
4. ビルド設定は次のとおり（**ビルドコマンドは不要**）

| 項目 | 値 |
|---|---|
| Production branch | `main` |
| Framework preset | なし（None） |
| Build command | **空欄** |
| Build output directory | **`site`** |

5. 「Save and Deploy」
6. `https://<プロジェクト名>.pages.dev` が発行される

以降、`main` に変更を反映すると自動で再公開されます。

| | |
|---|---|
| 費用 | **0円**（帯域無制限・SSL自動・ファイル2万件まで） |
| リスク | 無料条件が将来変わる可能性。ただしサイトは静的ファイルのみなので、GitHub Pagesやロリポップへ**数分で引っ越せます** |

### 旧URLからの転送（ロリポップを残す場合のみ）

ロリポップの契約が残っている間、現行サイトのルートに `.htaccess` を置きます。

```apache
RewriteEngine On
RewriteRule ^(.*)$ https://新しいURL/$1 [R=301,L]
```

現行サイトの主なURLと、V2での対応先は次のとおりです。

| 現行 | V2 |
|---|---|
| `/index.html` | `/index.html` |
| `/team.html` | `/club.html` |
| `/players.html` | `/players.html` |
| `/players*.html`（選手個別30ページ） | `/players.html`（一覧内のカードで詳細表示） |
| `/staff*.html`（6ページ） | `/players.html` |
| `/fixtures.html` | `/matches.html` |
| `/news.html` | `/news.html` |
| `/news_YYYY_article_NNN.html` | `/news-detail.html?id=nYYYYMMDD` |

### 参考：現行URLを維持する場合（採用しない）

`area-ohyaba.sub.jp` はロリポップの初期ドメインで、**他社サーバーへ向け直せません**。
維持するにはロリポップの契約（月額121円〜）が必要です。
今回は「現行URLの維持のためだけに有料契約はしない」方針のため、この案は採りません。
ただし**ロリポップをメール等の別用途でも使い続ける場合**は、追加費用0円でURLも維持できるため、
その事実が判明した時点で再検討する価値があります。

## 2. 公開前チェックリスト

- [ ] **選手・スタッフ名簿を最新化する**（現在のデータは2022年5月時点）
- [ ] スポンサー32社の**社名を確認する**（17社は現行HTMLに社名が無く、ロゴ画像のみ）
- [ ] スポンサーの**リンク先URLを確認する**（現在15社分のみ判明）
- [ ] 2024〜2026年の**試合結果を入力する**
- [ ] **サンプルの試合を削除する**（「試合」シートの「サンプル」列が「はい」の行）
- [ ] 「設定」シートの **「プレビュー帯を表示する」を「表示しない」** にする
- [ ] `site/robots.txt` の `Disallow: /` を解除し、`Sitemap:` 行を有効にする
- [ ] 全ページの `<meta name="robots" content="noindex,nofollow">` を削除する
- [ ] `canonical` と `og:url` `og:image` を**本番URLに書き換える**
- [ ] 選手の生年月日・身長・体重を公開するか決める（「設定」シート）
- [ ] Xアカウントを掲載するか決める（「設定」シートの「XのURL」。空なら非表示）
- [ ] 問い合わせ方法を決める（「設定」シートの「問い合わせメールアドレス」。空ならInstagram DMのみ）
- [ ] アクセス解析を入れるか決める（Cloudflare Web Analytics / GA4。どちらも無料）

## 3. Googleスプレッドシート連携

手順は **`SHEET_SETUP.md`** にまとめてあります（利用者の作業15〜20分・費用0円）。

- 取り込み用ファイル：`sheets/AREA_OHYABA_FC_サイトデータ.xlsx`（7シートを1ファイルに集約）
- スクリプト：`gas/Code.gs`
- サイト側の接続先：`site/assets/js/config.js` の `AO_API_URL`

`AO_API_URL` が空の間は同梱データ（`site/data/site-data.json`）で動きます。
連携後も、スプレッドシートが一時的に読めないときは同梱データへ自動的に切り替わります。

### 公開される情報の範囲

`gas/Code.gs` に書いてある列だけがサイトへ送られます。
シートに列を足しても、スクリプトに書かなければ**サイトには一切出ません**。
さらに「設定」シートの「選手の生年月日を公開する」等を「表示しない」にすると、
その項目は**サーバー側で削除してから**返すため、ブラウザにも届きません。

## 4. 元に戻したいとき

現行サイトの完全なコピーが `BACKUP/area-ohyaba.sub.jp/`（155ファイル・67MB）にあります。
このフォルダの中身をロリポップへアップロードし直せば、**現行サイトを完全に復元できます**。

---

## 5. 引き継ぎメモ

- サイトは**ただの静的ファイル**です。GitHub Pages・Cloudflare Pages・ロリポップ・
  その他どのサーバーへ置いても同じように動きます。**特定のサービスに縛られていません。**
- 引っ越しは `site/` フォルダをまるごとアップロードするだけです。
- 相対パスで作ってあるため、サブフォルダ配下（`https://example.com/fc/` など）でも動きます。
