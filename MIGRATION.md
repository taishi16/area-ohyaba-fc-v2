# 本番切替の手順（MIGRATION）

> **現行サイト https://area-ohyaba.sub.jp/ は、この文書の作業を実行するまで一切変更されません。**
> 実行はクラブオーナーの承認後に行います。

---

## 0. 現状の整理

| | 現行サイト | V2プレビュー |
|---|---|---|
| URL | https://area-ohyaba.sub.jp/ | https://taishi16.github.io/area-ohyaba-fc-v2/ |
| 置き場所 | ロリポップ！レンタルサーバー（GMOペパボ） | GitHub Pages |
| 費用 | **月額121円〜が発生している見込み**（無料プランなし） | 0円 |
| 更新方法 | HTMLを直接編集 | Googleスプレッドシート |
| 検索エンジン | 公開 | **非公開**（`noindex` + `robots.txt` で遮断中） |

`area-ohyaba.sub.jp` は**ロリポップの初期ドメイン**です。ロリポップがDNSを管理しており、
**他社サーバーへ向け直すことはできません。** そのため次の関係になります。

- **現行URLを維持する** ＝ ロリポップの契約を維持する（＝費用が続く）
- **月額0円にする** ＝ URLが変わる

---

## 1. 切替方法の選択肢

### A案：完全無料へ移行する（URLが変わる）

新URL: `https://area-ohyaba.pages.dev/` など（Cloudflare Pages）または現在のGitHub Pages URL。

1. 新URLでサイトを公開する
2. **ロリポップは即解約せず**、現行サイトのトップに新URLへの案内と自動転送を置く
3. Instagramプロフィールのリンクを新URLに変更する
4. 3〜6か月経ってからロリポップを解約する

| | |
|---|---|
| 費用 | **0円**（移行期間中はロリポップ費用が続くが、増えはしない） |
| 失うもの | 現行URL。ただし検索流入は現状ほぼ無いと見られる（sitemap無し・2年以上未更新・解析も停止中） |

### B案：現行URLを維持する（ロリポップ契約を続ける）

1. ロリポップのFTPで、現行ファイルを別フォルダへ退避
2. `site/` の中身をアップロード
3. `robots.txt` と各ページの `noindex` を解除

| | |
|---|---|
| 費用 | 月額121円〜が継続（**追加費用は0円**） |
| 利点 | URL・検索順位・既存リンクをそのまま維持できる |

> **ロリポップをメールなど他の用途でも使っていて今後も契約を続ける場合は、B案が最善です。**
> まず「ロリポップの契約者・プラン・更新日」をご確認ください。

### C案：独自ドメインを取得する

`area-ohyaba.com` などを取得（年1,000〜1,500円程度）し、Cloudflare Pages に接続する。
**0円ではありません。** クラブとして長期的にいちばん望ましい形ですが、費用の承認が必要です。

---

## 2. どの案でも共通してやること（公開前チェック）

- [ ] **選手・スタッフ名簿を最新化する**（現在のデータは2022年5月時点）
- [ ] スポンサー32社の**社名を確認する**（17社は現行HTMLに社名が無く、ロゴ画像のみ）
- [ ] スポンサーの**リンク先URLを確認する**（現在15社分のみ判明）
- [ ] 2024〜2026年の**試合結果を入力する**
- [ ] **サンプルの試合データを削除する**（MATCHESシートの `sample` が `TRUE` の行）
- [ ] CONFIGシートの **`preview_mode` を `FALSE`** にする
- [ ] `site/robots.txt` の `Disallow: /` を解除し、`Sitemap:` 行を有効にする
- [ ] 全ページの `<meta name="robots" content="noindex,nofollow">` を削除する
- [ ] `canonical` と `og:url` `og:image` を**本番URLに書き換える**
- [ ] 選手の生年月日・身長・体重を公開するか決める（CONFIGの `show_〜`）
- [ ] Xアカウントを掲載するか決める（CONFIGの `x_url`。空なら非表示）
- [ ] 問い合わせ方法を決める（CONFIGの `contact_email`。空ならInstagram DMのみ）
- [ ] アクセス解析を入れるか決める（Cloudflare Web Analytics / GA4。どちらも無料）

---

## 3. Googleスプレッドシート連携の設定手順（一度だけ）

1. Googleドライブで新しいスプレッドシートを作り、名前を「**AREA OHYABA FC サイトデータ**」にする
2. `sheets/` の7つのCSVを、**シート名をファイル名と同じ**にして取り込む
   （`MATCHES` `NEWS` `PLAYERS` `STAFF` `SPONSORS` `ACTIVITY` `CONFIG`）
   - 取り込みは「ファイル」→「インポート」→「アップロード」→**「新しいシートを挿入する」**
   - 取り込み後、シート名を CSV のファイル名（大文字）に合わせる
3. 「拡張機能」→「Apps Script」を開き、`gas/Code.gs` の内容をすべて貼り付けて保存
4. 「デプロイ」→「新しいデプロイ」→ 種類「**ウェブアプリ**」
   - 次のユーザーとして実行: **自分**
   - アクセスできるユーザー: **全員**
5. 表示された **ウェブアプリのURL** をコピー
6. `site/assets/js/config.js` の `window.AO_API_URL = "";` の `""` の中にそのURLを貼る
7. サイトを再公開する（`tools/deploy_preview.sh`）

> 手順6を行うまでは、サイトは同梱データ（`site/data/site-data.json`）で動きます。
> 連携後も、スプレッドシートが一時的に読めないときは同梱データへ自動的に切り替わります。

### 「全員がアクセスできる」設定について

このウェブアプリが返すのは、**PUBLIC_COLUMNSに書いた列だけ**です。
スプレッドシート本体は共有されません。個人情報を入れた列を作っても、
その列名をPUBLIC_COLUMNSに書かなければサイトへは一切送られません。

---

## 4. 旧サイトからのリダイレクト（A案を選んだ場合）

ロリポップの契約が残っている間、現行サイトのルートに `.htaccess` を置きます。

```apache
# 旧サイト → 新サイトへの転送
RewriteEngine On
RewriteRule ^(.*)$ https://新しいURL/$1 [R=301,L]
```

現行サイトの主なURLと、V2での対応先は次のとおりです。

| 現行 | V2 |
|---|---|
| `/index.html` | `/index.html` |
| `/team.html` | `/club.html` |
| `/players.html` | `/players.html` |
| `/players*.html`（選手個別30ページ） | `/players.html`（一覧内のカードで詳細表示） |
| `/staff*.html`（6ページ） | `/players.html` |
| `/fixtures.html` | `/matches.html` |
| `/news.html` | `/news.html` |
| `/news_YYYY_article_NNN.html` | `/news-detail.html?id=N00X` |

---

## 5. 元に戻したいとき

現行サイトの完全なコピーが `BACKUP/area-ohyaba.sub.jp/`（155ファイル・67MB）にあります。
このフォルダの中身をロリポップへアップロードし直せば、**現行サイトを完全に復元できます**。

---

## 6. 引き継ぎメモ

- サイトは**ただの静的ファイル**です。GitHub Pages・Cloudflare Pages・ロリポップ・
  その他どのサーバーへ置いても同じように動きます。**特定のサービスに縛られていません。**
- 引っ越しは `site/` フォルダをまるごとアップロードするだけです。
- 相対パスで作ってあるため、サブフォルダ配下（`https://example.com/fc/` など）でも動きます。
